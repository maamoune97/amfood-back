<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-5">
        <a class="navbar-brand" href="{{ path('home_index') }}">AM Delivery</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03"
            aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    
        <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('home_index') }}">Dashboard</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="{{path('restaurant_index')}}">Restaurants</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="{{path('delivery_man_index')}}">Livreurs</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="{{path('admin_restaurant_manager_index')}}">Managers</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="{{path('city_index')}}">Villes</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                {% if not app.user %}
                    <li class="nav-item">
                        <a href="{{path('account_login')}}" class="nav-link">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="nav-link">Inscription</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{path('account_logout')}}" class="nav-link">Deconnexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
</header>
